import {STATE_KEY} from './populations.const';

export const populationsIdsList = (state) => state[STATE_KEY].populationsIdsList;
export const currentPopulation = (state) => state[STATE_KEY].currentPopulation;